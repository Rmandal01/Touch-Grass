name: growflow
description: GrowFlow - productivity garden. Screen-time productivity score slice.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  # Pulls real per-app screen time on Android via UsageStatsManager.
  app_usage: ^4.1.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
